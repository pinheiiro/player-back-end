spring:
  application:
    name: musica
  datasource:
    url: jdbc:postgresql://212.85.0.229:5432/player
    username: gabrielp
    password: pinheiro1900
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080